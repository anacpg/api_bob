import assert from 'assert';
import { C } from '../../src/common';
import { RequestProvider } from '../../src/providers';

const URL = 'http://localhost:4000';

export default function () {
  it('list', async () => {

  });

  it('add', async () => {

  });
}
